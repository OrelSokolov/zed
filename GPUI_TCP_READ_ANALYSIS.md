# Как GPUI влияет на замедление TCP read

## Архитектура GPUI

### 1. Event Loop (Wayland/X11)
**Место**: `crates/gpui/src/platform/linux/wayland/client.rs:853-859`

```rust
event_loop.run(
    None,  // timeout = None означает бесконечное ожидание
    &mut WaylandClientStatePtr(...),
    |_| {},
)
```

**Как работает**:
- Event loop блокируется на `epoll_wait()` (через calloop)
- Ожидает событий от Wayland/X11
- Когда событие приходит, обрабатывает его и вызывает `runnable.run()`
- **Timeout = None** означает, что event loop блокируется до появления события

**Влияние на TCP read**:
- ❌ **НЕ влияет напрямую** - event loop работает на главном потоке
- ✅ Наш поток `ollama-stream-reader` работает независимо

### 2. Background Executor Worker Threads
**Место**: `crates/gpui/src/platform/linux/dispatcher.rs:48-88`

```rust
std::thread::spawn(move || {
    for runnable in receiver.iter() {  // Последовательная обработка
        runnable.run();  // Если блокируется, другие задачи ждут
    }
})
```

**Как работает**:
- Каждый worker thread обрабатывает задачи **последовательно**
- Если одна задача блокируется на I/O, весь поток блокируется
- Количество потоков = `available_parallelism().max(2)` (на 8 ядрах = 8)

**Влияние на TCP read**:
- ❌ **НЕ влияет напрямую** - мы используем отдельный поток
- ⚠️ **Может влиять косвенно** - если все worker threads заняты, планировщик ОС может откладывать наш поток

### 3. Foreground Executor (главный поток)
**Место**: `crates/gpui/src/platform/linux/wayland/client.rs:500-549`

```rust
handle.insert_idle(|_| {
    runnable.run();  // Выполняется на главном потоке
});
```

**Как работает**:
- Все foreground задачи выполняются на главном потоке
- Обрабатываются через `insert_idle()` в event loop
- Частота обработки зависит от частоты событий Wayland/X11

**Влияние на TCP read**:
- ❌ **НЕ влияет напрямую** - мы используем отдельный поток
- ⚠️ **Может влиять косвенно** - если главный поток занят, это может влиять на планировщик ОС

## Проблема: Планировщик ОС (CFS)

### Как CFS работает

1. **Виртуальное время (vruntime)**:
   - Каждый поток имеет `vruntime` - виртуальное время выполнения
   - CFS выбирает поток с **наименьшим vruntime** для выполнения

2. **Распределение CPU времени**:
   - CFS стремится равномерно распределить CPU между всеми потоками
   - При большом количестве потоков каждый получает меньше времени

3. **Пробуждение потоков**:
   - Когда поток блокируется на I/O (например, `read()`), он уходит в сон
   - Когда данные готовы, поток пробуждается
   - **НО**: CFS может отложить пробуждение, если есть другие потоки с более высоким приоритетом

### Почему наш поток медленный

**На 8-ядерной системе в Zed**:
- Rayon: 4 потока
- GPUI Background: 8 потоков  
- Timer: 1 поток
- Clipboard: 1 поток
- Главный поток: 1 поток
- **Итого: ~15 потоков**

**Проблема**:
1. CFS распределяет CPU между всеми 15 потоками
2. Когда наш поток блокируется на `read()`, он уходит в сон
3. Когда данные готовы, поток пробуждается
4. **НО**: CFS может отложить пробуждение на 40-50мс, чтобы дать время другим потокам
5. Это объясняет, почему `read()` занимает 40-45мс вместо 5-7мс

### Почему test_ollama.rs быстрый

- **1 поток** - нет конкуренции
- CFS сразу пробуждает поток при готовности данных
- `read()` занимает 5-7мс (реальное время ожидания данных)

## Вывод

**GPUI НЕ влияет напрямую** на TCP read в нашем потоке, но:

1. **Косвенное влияние через планировщик ОС**:
   - Большое количество потоков создает конкуренцию за CPU
   - CFS откладывает пробуждение нашего потока на 40-50мс
   - Это объясняет задержки в `read()`

2. **Решения**:
   - ✅ CPU affinity (уже добавлено) - привязка к последнему ядру
   - ✅ Отдельный поток вне async контекста (уже сделано)
   - 🔄 Уменьшить количество worker threads (можно попробовать)
   - 🔄 Использовать `ionice` для I/O приоритета (можно попробовать)
   - ⚠️ Использовать `SCHED_FIFO` (требует root)

## Рекомендации

1. **Проверить perf trace с фильтром по TID** - увидеть реальные вызовы `read()`
2. **Попробовать уменьшить количество worker threads** - освободить CPU
3. **Проверить, не блокируются ли worker threads** - могут создавать дополнительную нагрузку на планировщик
